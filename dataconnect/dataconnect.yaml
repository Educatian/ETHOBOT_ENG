specVersion: "v1beta"
serviceId: "aichat"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aichat-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
